#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
    char str[80];
    char revstr[80];

    print("Enter stirng to be reversed: ");
    gets(str);

    for (int i = 0; i < strlen(str); i++)
    {
        revstr[i] = str
    }
}